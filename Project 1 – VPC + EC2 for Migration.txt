Start Project 1 – VPC + EC2 for Migration (2–3 hours, Today)
Since you’re confident with Terraform/CFT for any service, let’s dive into Project 1: VPC + EC2 Setup for Migration to showcase your IaC skills for a migration use case (like MGN). This portfolio piece will impress clients and align with your job’s focus. Use AWS Free Tier to avoid costs.

Project Details:

What: Write a Terraform or CFT template to:
Create a VPC with 1 public subnet, 1 private subnet, an Internet Gateway, and Route Tables.
Launch an EC2 instance in the public subnet with a Security Group (port 80 open for HTTP).
Deploy a simple “Hello World” webpage on EC2 (mimics a migrated app).
Why: Shows your IaC skills (Terraform/CFT), migration-ready infra (VPC/EC2), and real-world use (web app hosting). Perfect for $80–$150 gigs.
Output: GitHub repo with code, README, and screenshots of the VPC, EC2, and webpage.
Steps for Today (2–3 hours):

AWS Free Tier Setup (10 mins):
Log into AWS Free Tier (confirm 750 EC2 hours/month).
Set a billing alert at $5 (AWS Console > Billing > Budgets > Create Budget).
Task: Reply with “AWS login done” or any issues.
GitHub Setup (10 mins):
Create a GitHub repo named “aws-migration-portfolio” (or similar).
Install GitHub Desktop (free) or use CLI if comfy.
Task: Reply with “GitHub ready” or any setup help needed.
Write Terraform/CFT Code (1.5–2 hours):
Since you’re confident with both, pick Terraform (more marketable) or CFT (AWS-native, your Western Union experience).
Terraform Example (simplified, I can share full code if needed):
hcl

Copy
provider "aws" {
  region = "us-east-1"
}
resource "aws_vpc" "migration_vpc" {
  cidr_block = "10.0.0.0/16"
  tags = { Name = "Migration-VPC" }
}
resource "aws_subnet" "public_subnet" {
  vpc_id = aws_vpc.migration_vpc.id
  cidr_block = "10.0.1.0/24"
  availability_zone = "us-east-1a"
  tags = { Name = "Public-Subnet" }
}
resource "aws_internet_gateway" "igw" {
  vpc_id = aws_vpc.migration_vpc.id
  tags = { Name = "Migration-IGW" }
}
resource "aws_route_table" "public_rt" {
  vpc_id = aws_vpc.migration_vpc.id
  route {
    cidr_block = "0.0.0.0/0"
    gateway_id = aws_internet_gateway.igw.id
  }
  tags = { Name = "Public-Route-Table" }
}
resource "aws_route_table_association" "public_assoc" {
  subnet_id = aws_subnet.public_subnet.id
  route_table_id = aws_route_table.public_rt.id
}
resource "aws_security_group" "web_sg" {
  vpc_id = aws_vpc.migration_vpc.id
  ingress {
    from_port = 80
    to_port = 80
    protocol = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }
  egress {
    from_port = 0
    to_port = 0
    protocol = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }
  tags = { Name = "Web-SG" }
}
resource "aws_instance" "web_server" {
  ami = "ami-0c55b159cbfafe1f0" # Amazon Linux 2 (Free Tier, us-east-1)
  instance_type = "t2.micro"
  subnet_id = aws_subnet.public_subnet.id
  security_groups = [aws_security_group.web_sg.name]
  user_data = <<-EOF
              #!/bin/bash
              yum update -y
              yum install httpd -y
              systemctl start httpd
              systemctl enable httpd
              echo "<h1>Hello World from $(hostname -f)</h1>" > /var/www/html/index.html
              EOF
  tags = { Name = "Migration-EC2" }
}
output "web_url" {
  value = "http://${aws_instance.web_server.public_ip}"
}
What It Does: Creates a VPC, public subnet, Internet Gateway, Route Table, Security Group, and EC2 instance with a “Hello World” webpage.
CFT Alternative: If you prefer CFT, I can share a YAML/JSON version (same setup).
Task: Create a file (e.g., main.tf or template.yaml), paste the code (or write your own), and test it in AWS Console (Terraform: terraform init, apply; CFT: upload to CloudFormation). Reply with “Code written” or any errors.
Test & Screenshot (30 mins):
Deploy the template (AWS Console or CLI).
Visit the EC2’s public IP (outputted URL) to confirm the “Hello World” webpage.
Take screenshots: VPC (AWS Console > VPC), EC2 instance, webpage.
Task: Reply with “Deployed, webpage up!” or issues. Save screenshots locally.
Output by End of Today:

Terraform/CFT code in a GitHub repo (commit but don’t push yet).
Screenshots of VPC, EC2, and webpage (save to a folder).
Task: Push to GitHub tomorrow after testing.
Tools for Today
AWS Free Tier: For Project 1 (set billing alert at $5).
GitHub: Create repo (free).
Terraform: Install (free, download) or use AWS CloudFormation Console.
VS Code: For writing code (free).
Notepad: For 3 services and goal note.